function onCreate() {
    character.loadXML();
}

function update(elapsed) {
    var animsToSpeedUp = [
        "singUP-alt",
        "singDOWN-alt"
    ];
    if(animsToSpeedUp.contains(character.animation.name))
        character.animation.curAnim.frameRate = 24 * FlxG.sound.music.pitch;
}