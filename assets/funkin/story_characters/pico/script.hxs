function create() {
    frames = FNFAssets.getStoryCharacterSparrow("pico");
    scale.set(0.65, 0.65);
    updateHitbox();
    animation.addByPrefix("idle", "Pico Idle Dance", 24, false);

    flipX = true;

    setOffset("idle");

    dance();

    x -= 70;
    y -= 20;
}