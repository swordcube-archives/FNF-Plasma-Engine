function create() {
    frames = FNFAssets.getStoryCharacterSparrow("tankman");
    animation.addByPrefix("idle", "tank idle", 24, false);

    setOffset("idle");

    dance();

    x += 50;
    y += 50;
}