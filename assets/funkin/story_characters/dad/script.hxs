function create() {
    frames = FNFAssets.getStoryCharacterSparrow("dad");
    scale.set(0.5, 0.5);
    updateHitbox();
    animation.addByPrefix("idle", "Dad idle dance BLACK LINE", 24, false);
    setOffset("idle");

    dance();

    x -= 70;
    y -= 180;
}