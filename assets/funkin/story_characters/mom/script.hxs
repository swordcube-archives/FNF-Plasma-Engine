function create() {
    frames = FNFAssets.getStoryCharacterSparrow("mom");
    scale.set(0.45, 0.45);
    updateHitbox();
    animation.addByPrefix("idle", "Mom Idle BLACK LINES", 24, false);
    setOffset("idle");

    dance();

    x -= 30;
    y -= 220;
}